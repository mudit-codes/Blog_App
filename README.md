# Flask Blog App

Local dev: create venv, install requirements, run `python app.py`.

Default admin created on first run:
- username: admin
- password: admin123

Do NOT deploy with these credentials to production.
